# Attach the application stack to the Hatchet backend network.
#
# Prerequisites:
#   docker network create backend
#   cd docker/hatchet && docker compose up -d
#
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.hatchet-network.yml up -d
#
# Set in docker-compose.yml (x-manus-app-env):
#   HATCHET_CLIENT_HOST_PORT: hatchet-engine:7070
#   HATCHET_CLIENT_TLS_STRATEGY: none
#   HATCHET_CLIENT_TOKEN: <token from http://localhost:8888>

services:
  manus-web:
    networks:
      - manus-network
      - backend

  manus-worker:
    networks:
      - manus-network
      - backend

networks:
  backend:
    external: true
