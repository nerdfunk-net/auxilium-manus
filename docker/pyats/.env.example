# Copy to .env and set a random secret (e.g. `openssl rand -hex 32`).
# This is the bearer token pyats-shim requires on every /v1/jobs call; paste
# the same value into the pyATS source's "Token" field in Settings → Sources.
PYATS_SHIM_TOKEN=
