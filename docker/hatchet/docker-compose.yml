# Auxilium Manus — Hatchet workflow infrastructure
#
# Services:
#   hatchet-*     — workflow orchestration engine (dashboard: http://localhost:8888)
#
# Prerequisites (once):
#   docker network create internal
#   docker network create --internal hatchet
#
# Hatchet first-time setup:
#   1. cd docker/hatchet && docker compose up -d
#   2. Wait ~60s for hatchet-setup-config to complete and hatchet-dashboard to start
#   3. Open http://localhost:8888, sign in (admin@example.com / Admin1234!)
#   4. Settings → API Tokens → Create Token → copy token
#   5. In docker/docker-compose.yml (x-manus-app-env), set:
#        HATCHET_CLIENT_TOKEN: <paste token here>
#        HATCHET_CLIENT_HOST_PORT: hatchet-engine:7070
#   6. Start the app stack — see docker/README.md
#
# Networks:
#   internal — Hatchet private infra (postgres, rabbitmq, migrate, setup)
#   hatchet  — isolated (--internal): engine/dashboard ↔ manus-web / manus-worker

services:
  # ─── Hatchet infrastructure ──────────────────────────────────────────────────
  # App postgres/redis live in docker/docker-compose.yml (not needed here).
  # Hatchet uses hatchet-postgres + RabbitMQ for its own state/messaging.
  hatchet-postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    networks:
      - internal
    environment:
      POSTGRES_USER: hatchet
      POSTGRES_PASSWORD: hatchet
      POSTGRES_DB: hatchet
    volumes:
      - hatchet_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hatchet"]
      interval: 5s
      timeout: 5s
      retries: 10

  hatchet-rabbitmq:
    image: rabbitmq:3-management
    restart: unless-stopped
    networks:
      - internal
    environment:
      RABBITMQ_DEFAULT_USER: hatchet
      RABBITMQ_DEFAULT_PASS: hatchet
    volumes:
      - hatchet_rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 10s
      retries: 5

  # Step 1: run DB migrations
  hatchet-migrate:
    image: ghcr.io/hatchet-dev/hatchet/hatchet-migrate:v0.105.2
    command: /hatchet/hatchet-migrate
    depends_on:
      hatchet-postgres:
        condition: service_healthy
    networks:
      - internal
    environment:
      DATABASE_URL: "postgres://hatchet:hatchet@hatchet-postgres:5432/hatchet"

  # Step 2: generate encryption keys, seed admin user (skips cert generation)
  hatchet-setup-config:
    image: ghcr.io/hatchet-dev/hatchet/hatchet-admin:v0.105.2
    command: /hatchet/hatchet-admin quickstart --skip certs --generated-config-dir /hatchet/config --overwrite=false
    depends_on:
      hatchet-migrate:
        condition: service_completed_successfully
      hatchet-rabbitmq:
        condition: service_healthy
      hatchet-postgres:
        condition: service_healthy
    networks:
      - internal
    environment:
      DATABASE_URL: "postgres://hatchet:hatchet@hatchet-postgres:5432/hatchet"
      SERVER_MSGQUEUE_RABBITMQ_URL: "amqp://hatchet:hatchet@hatchet-rabbitmq:5672/"
      SERVER_AUTH_COOKIE_DOMAIN: "localhost:8888"
      SERVER_AUTH_COOKIE_INSECURE: "t"
      SERVER_GRPC_BIND_ADDRESS: "0.0.0.0"
      SERVER_GRPC_INSECURE: "t"
      SERVER_GRPC_BROADCAST_ADDRESS: "localhost:7077"
      SERVER_DEFAULT_ENGINE_VERSION: "V1"
      SERVER_INTERNAL_CLIENT_INTERNAL_GRPC_BROADCAST_ADDRESS: "hatchet-engine:7070"
      SERVER_OBSERVABILITY_ENABLED: true
    volumes:
      - hatchet_config:/hatchet/config
      - hatchet_certs:/hatchet/certs

  # Step 3: engine (gRPC on :7070 via the isolated hatchet network; host :7077 for bare-metal clients)
  hatchet-engine:
    image: ghcr.io/hatchet-dev/hatchet/hatchet-engine:v0.105.2
    command: /hatchet/hatchet-engine --config /hatchet/config
    restart: on-failure
    depends_on:
      hatchet-migrate:
        condition: service_completed_successfully
      hatchet-setup-config:
        condition: service_completed_successfully
    networks:
      - internal
      - hatchet
    environment:
      DATABASE_URL: "postgres://hatchet:hatchet@hatchet-postgres:5432/hatchet"
      SERVER_GRPC_BIND_ADDRESS: "0.0.0.0"
      SERVER_GRPC_INSECURE: "t"
      SERVER_OBSERVABILITY_ENABLED: true
    volumes:
      - hatchet_config:/hatchet/config
      - hatchet_certs:/hatchet/certs
    ports:
      - "7077:7070"

  # Step 4: dashboard (API + UI combined, port 8888)
  hatchet-dashboard:
    image: ghcr.io/hatchet-dev/hatchet/hatchet-dashboard:v0.105.2
    command: sh ./entrypoint.sh --config /hatchet/config
    restart: on-failure
    depends_on:
      hatchet-migrate:
        condition: service_completed_successfully
      hatchet-setup-config:
        condition: service_completed_successfully
    networks:
      - internal
      - hatchet
    environment:
      DATABASE_URL: "postgres://hatchet:hatchet@hatchet-postgres:5432/hatchet"
      SERVER_OBSERVABILITY_ENABLED: true
    volumes:
      - hatchet_config:/hatchet/config
      - hatchet_certs:/hatchet/certs
    ports:
      - "8888:80"

volumes:
  hatchet_postgres_data:
  hatchet_rabbitmq_data:
  hatchet_config:
  hatchet_certs:

networks:
  hatchet:
    external: true
  internal:
    external: true
